<div class="tab armour" data-group="primary" data-tab="armour">
  <table class="fixed_header">
    <thead>
      <tr>
        <th class="item-image"></th>
        <th class="align-left armour-name sortable">{{localize "wh4e.item.name"}}</th>
        <th class="armour-ac">{{localize "wh4e.item.armourClass"}}</th>
        <th class="equippable">{{localize "wh4e.item.equipped"}}</th>
        {{> controlsEditDeleteHeader title=(localize "wh4e.sheet.addArmour") type="Armour"}}
      </tr>
    </thead>
    <tbody class="item-list">
      {{#each armour as |armour id|}}
        <tr data-item-id="{{armour._id}}" class="item">
          <td class="item-image">
            <a class="item-description">
              <span class="item-info" style="background-image: url({{armour.img}})"></span>
            </a>
          </td>
          <td class="align-left armour-name" title="{{{stripHtml armour.system.description}}}">
            <span>{{armour.name}}</span>
          </td>
          <td class="armour-ac">
            <span>{{getTextFromKey "wh4e.armourClasses" armour.system.armourClass}}</span>
          </td>
          {{> controlsEquipStore name=armour.name status=armour.system.equippedStatus}}
          {{> controlsEditDelete editTitle=(localize "wh4e.sheet.editArmour") deleteTitle=(localize
        "wh4e.sheet.deleteArmour") }}
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>